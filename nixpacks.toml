[phases.setup]
nixPkgs = ["nodejs_18", "python3", "ffmpeg"]

[phases.install]
cmds = [
  "npm install",
  "pip install yt-dlp",
  "ln -s $(which python3) /usr/bin/python || true"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
